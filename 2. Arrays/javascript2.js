// ARRAY
let array = ['jonathan', 'steve', 'javier', 'julio', 'ariana']

// METODOS DE UN ARRAY

    // Permite agregar un valor al final del array
    array.push("vladimir")

    // Permite quitar el último elemento del array
    array.pop()

    // Permite agregar un valor al inicio del array
    array.unshift("edgar")

    // Permite quitar el primer elemento
    array.shift()

    // Agrega y quita elementos por indices
    array.splice(1,2,"vladimir","edgar")

    // Extrae en un nuevo array desde 1 hasta 3
    array.slice(1,3)

    // Invierte el orden de los elementos
    array.reverse()
